language: node_js
node_js:
  - node
script:
  - node --version && npm --version && yarn --version
  - yarn build:ci
after_success:
  - yarn codecov
  - npm run travis-deploy-once "npm run semantic-release"
os:
  - linux